
@using printstreamer.Components.Shared
@inherits LayoutComponentBase

<PageTitle>PrintStreamer</PageTitle>

<style>
    body { margin: 0; font-family: sans-serif; background: #111; color: #eee; }
    .wrap { padding: 10px; margin: 0; }
    .nav { margin-bottom: 20px; padding: 10px 0; border-bottom: 1px solid #444; display: flex; gap: 16px; align-items: center; }
    .nav a { color: #66ccff; text-decoration: none; padding: 6px 10px; font-size: 1.1em; border-radius: 6px; }
    .nav a:hover { text-decoration: underline; }
    .nav a.active { color: #fff; font-weight: bold; background: rgba(102,204,255,0.04); }
</style>


@inject NavigationManager Nav
@inject IJSRuntime JS

<div class="wrap">
    <div class="nav">
    <NavLink href="/" Match="NavLinkMatch.All">Console 🖨️</NavLink>
        <NavLink href="/streaming">Stream 📺</NavLink>
        <NavLink href="/timelapses">Timelapses ⌛</NavLink>
        <NavLink href="/audio">Audio 🎵</NavLink>
        <NavLink href="/config">Configuration ⚙️</NavLink>
    </div>

    <PrinterUiIframes @rendermode="@(new Microsoft.AspNetCore.Components.Web.InteractiveServerRenderMode())" />

    @Body
</div>

@code {
    // The persistent iframe stack is now in a child component for clarity
}
